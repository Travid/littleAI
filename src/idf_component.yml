version: "1.0.0"

dependencies:
  idf: ">=5.3"

  # Managed components
  lvgl/lvgl:
    version: "8.4.*"
    public: true

  # Use the vendored Waveshare panel + touch components from ./components
  esp_lcd_touch_ft5x06:
    version: "*"
    public: true

  # Needed by the Waveshare reference design (power/reset sequencing for panel+touch)
  espressif/esp_io_expander_tca9554:
    version: "^1.0.1"
    public: true

  # Onboard ES8311 codec (speaker + mic)
  espressif/es8311:
    version: "^1.0.0"
    public: true

  esp_lcd_sh8601:
    path: "../components/esp_lcd_sh8601"
    version: "*"
    public: true
